# Transformer Transformer 따라하기 설명서

논문: *Transformer Transformer: A Unified Model for Motion-Conditioned Robot Co-design*
(Huy Ha, C. Karen Liu, Shuran Song — Stanford / Columbia, CoRL 2026, arXiv 2607.25798)
프로젝트 페이지: https://transformer-transformer.github.io · 코드: 이 폴더 (`real-stanford/transformer-transformer`, commit `30fb79e`)

이 문서는 논문 26쪽 전체, repo `README.md`, `docs/` 5개 문서, 핵심 config 를 읽고 정리했다 (2026-09-13).
**이 서버에서 직접 실행해 검증한 명령은 아직 없다.** 명령은 repo 문서 기준이고, 이 서버(V100)에서 달라지는 부분은
각 단계에 `[V100]` 으로 표시했다.

---

## 0. 한눈에 보기

### 이 논문이 하는 일

사람이 손으로 보여준 **말단장치(그리퍼) 궤적** 하나를 주면, 그 동작을 가장 잘 따라 하는 **로봇 설계 전체**
(링크 길이·질량·관성, 관절 종류·축, 모터 게인·토크 한계, 장착 위치)를 **몇 초 만에 생성**한다.
같은 모델이 그 로봇을 **제어**도 하고, 설계가 좋은지 **점수**도 매긴다.

| 기존 co-design (예: CMA-ES) | Transformer Transformer |
|---|---|
| 설계 후보 → 시뮬레이터에서 컨트롤러로 수천 step 롤아웃 → 점수 → 반복 | 확산 모델이 설계 + 그 설계의 동역학(상태·행동)을 **한 번에** 생성 |
| 보상 바꾸면 다시 최적화 | 학습 때 본 적 없는 보상도 **추론 시점에** 바로 적용 (zero-shot) |
| 26 궤적 bimanual: 11,505 s | 같은 문제: 20.7 s (Zeroth-Order) |

### 따라 하면 무엇이 남는가

| 단계 | 하는 일 | 결과물 | 배우는 것 |
|---|---|---|---|
| 0 | 환경 구성 | `.venv`, GPU 인식 확인 | JAX + PyTorch + MuJoCo 스택, CUDA 호환성 |
| 1 | RoboToken 왕복 테스트 | `811 passed`, `wheeled_bimanual.zarr` | 로봇을 토큰으로 바꾸는 방법 (논문 §2.1) |
| 2 | 체크포인트·데이터 다운로드 | `checkpoints/`, `data/` | 공개 자산 구성 |
| 3 | 교차 형상 제어 평가 | `pos_err`, 생존률 | 형상 조건부 단일 제어기 (§2.2 b) |
| 4 | 형상 생성 (Zeroth-Order) | 궤적별 최적 로봇, `predicted` vs `actual` | 생성기 = 비평가 (§2.2 a) |
| 5 | 보상 교체 + Dynamics Self-Guidance | 보상별로 다른 로봇, 성능 비교 | zero-shot 보상 최적화 (§2.3) |
| 6 | 확산 과정 시각화 | 노이즈 → 로봇 mp4 | 확산 모델이 로봇을 "그리는" 과정 |
| 7 (선택) | 작은 데이터 생성 + 짧은 학습 | 내 체크포인트 | 데이터 파이프라인과 학습 레시피 |
| 8 (선택) | CMA-ES 기준선 | 같은 문제의 CMA-ES 결과 | 논문 Fig. 5 의 비교가 무엇인지 |

단계 1~6 은 공개 체크포인트만 쓰므로 **학습 없이** 논문의 세 가지 주장(표현·통합 모델·zero-shot 최적화)을 직접 확인할 수 있다.

---

## 1. 논문 핵심 정리

### 1.1 문제 설정 — Motion-conditioned robot co-design

- **입력 1:** 목표 말단장치 궤적 `{p_target^t, o_target^t}` (50 Hz SE(3) pose). 사람이 UMI/iPhUMI 그리퍼를 들고 시연해 얻는다.
- **입력 2:** 사용자 보상 함수. 추적 정확도 + (토크, 관절속도, 크기, 질량) 페널티.
- **출력:** 보상을 최대화하는 **완전한** 로봇 형상 (MJCF 로 복원 가능).

추적 보상 (논문 식 1, `config/reward_fns/tracking_only.yaml`):

```
R = Σ_t exp( -‖e_p‖²/σ_p - ‖e_o‖²/σ_o ),   σ_p = 0.01, σ_o = 0.5
```

위치 오차가 0.5 m (quadruped 0.8 m) 를 넘거나 넘어지면 episode 종료.
변형 보상은 `max(exp(...) - α‖τ‖², 0)` (토크), `α‖q̇‖²` (속도), `-Tα·min(size - target, 0)` (크기) 등.

### 1.2 RoboToken — 로봇을 토큰 시퀀스로 (§2.1, Table 19)

MJCF 한 파일을 **연속값 벡터 토큰들의 집합**으로 바꾼다.

| 토큰 | 들어가는 속성 (차원) | 시간 |
|---|---|---|
| Link | geom 종류(3, binary), 크기(3), 질량(1, log), 관성 위치·회전·대각(3+9+3), 마찰, 색, free/track link ID | 불변 |
| Dynamic Joint | 관절 종류, 범위, armature·damping·friction·stiffness(log), link 에 대한 위치·회전, Link ID(7, binary) | 불변 |
| Fixed Joint | 위치, 회전, Link ID | 불변 |
| Actuator | ctrl/force range, Kp·Kv(log), gear, 종류, Dyna Joint ID | 불변 |
| Free/Track Link Obs | 위치(3) + 회전(9) | 매 step |
| Dynamic Joint Obs | qpos, qvel | 매 step |
| Actuator Obs | 속도, 힘 | 매 step |
| Control | 목표 관절 위치 | 매 step |
| Target Pose | 목표 말단 pose (조건, 노이즈 안 섞음) | 매 step |

설계 원칙 5 가지:

1. **Complete** — 기구학·기하·동역학 전부 포함. 그래서 크기·질량 같은 보상을 토큰에서 바로 계산할 수 있다.
2. **Flexible** — 토큰끼리 ID 로 가리킨다 (관절→두 링크, 모터→관절). 관절 수·연결 구조가 달라도 같은 형식.
   ID 는 모델에서 학습되는 positional embedding 이 된다.
3. **Consistent** — 전처리로 모든 transform 을 관절 토큰에 몰고, 링크마다 primitive geom 하나로 쪼갠다.
   여러 geom 인 링크는 균일 밀도 가정 + 평행축 정리로 관성을 나눈다.
4. **Extensible** — 새 필드 추가 경로가 정해져 있다 (`docs/extending.md`).
5. **Optimizable** — 연속값이라 확산과 gradient 가 가능하다. MJCF 텍스트를 LLM 으로 생성하면 이게 안 된다.

MuJoCo Menagerie 11 개 로봇 (0.65 kg Allegro ~ 67.5 kg ANYmal C, 6~35 DoF) 이 28~101 토큰, MJCF 텍스트 대비 27~110배 짧다.
**토큰화는 가역적이다** (`tokenize()` ↔ `detokenize()`). 확산으로 만든 토큰을 바로 시뮬레이션할 수 있는 이유.

### 1.3 모델 — DiT 하나, 마스킹만 바꿔 세 역할 (§2.2, Fig. 4)

- 백본: Diffusion Transformer (DiT). 토큰 타입별 선형 투영 + ID 임베딩 + 시간 ID, 확산 step 은 AdaLN 으로.
- 타입별로 최대 길이까지 padding 후 한 줄로 이어 붙인다. 짧은 로봇은 **padding 토큰 자체를 예측**하게 학습.
- 에피소드 전체가 아니라 **8 개 timestep 만** 뽑아 학습 (추론이 빠른 이유).
- SE(2) 평면 변환 augmentation (중력 때문에 SE(3) 아님).

| 역할 | 노이즈를 섞는 토큰 | 조건으로 주는 토큰 | DDIM step |
|---|---|---|---|
| **Motion-to-Robot** (`hardware_gen`) | 형상 + 상태 + 행동 전부 | 목표 궤적 | 100 |
| **Cross-Embodiment Control** (`ctrl`) | 행동 (quadruped 는 미래 상태도) | 형상 + 현재 상태 + 목표 궤적 | 5 |
| Unconditional | 전부 | 없음 | — |

핵심 아이디어: `hardware_gen` 은 **로봇과 "그 로봇이 동작을 수행하는 궤적"을 함께** 생성한다.
그러니 생성된 상태·행동으로 보상을 바로 계산할 수 있다 → **생성기가 곧 비평가(critic)**.
보상 전용 value 를 학습하지 않고 **보상 무관한 동역학**을 학습했기 때문에, 학습 때 없던 보상도 쓸 수 있다.

### 1.4 두 가지 최적화 방법

**Zeroth-Order Optimizer** (`config/hardware_optimizer/zeroth_order.yaml`)
- 후보 `num_seeds=64` 개를 한 배치로 병렬 확산 → 각자 예측한 동역학으로 보상 계산 → 최고 1 개 선택.

**Dynamics Self-Guidance, DSG** (§2.3, `guided_diffusion.yaml`)
- 매 denoising step 에서 예측 동역학으로 계산한 보상의 gradient 를 **형상 토큰**(link, dyna_joint, fixed_joint, actuator)에 흘려
  classifier guidance 처럼 노이즈 예측을 보정한다.
- 별도 동역학 모델이나 미분 가능 시뮬레이터가 필요 없다 (선행연구와의 차이).
- 실무 설정: DDIM `eta=1.0` (확률적 샘플링이 DSG 성능을 크게 올림, zeroth-order 에는 영향 없음),
  `guidance.scale` 은 design space 마다 다름 (ViperX 50, ViperX multi 500, quadruped 100, bimanual 0.2).
- 형상 토큰에 직접 걸리는 보상(크기·질량)은 gradient 가 커서 guidance 계수를 0.005 로 낮춰야 설계가 깨지지 않는다.
- 결론 (§3): **샘플이 적을 때 DSG 가 크게 이기고** (bimanual 단일 샘플 242 → 341), 샘플이 많으면 둘이 같아진다.

**다중 궤적 최적화:** 26 개 궤적의 노이즈 예측을 매 step 평균 (diffusion composition) → 설계 하나가 전부를 커버.

### 1.5 데이터

| Design space | 무엇을 시험하나 | 이산 선택 | 연속 변수 | 전문가 제어기 | 데이터 규모 |
|---|---|---|---|---|---|
| ViperX 팔 | 기구학 설계 (도달성) | 장착 방향 4, DoF 5/6/7, 관절축 3 | 장착 위치, 링크 길이 -5~+20 cm | Mink DiffIK | 3.8M ep, 380K 형상 |
| Quadruped + 팔 | 동적 전신 제어 | 7 binary (다리 serial/linkage ×4, 무릎 방향, 팔 레일) = 128 | 다리·몸통·팔 길이, 배터리 위치 | PPO 전문가 128 개 (각 16 A100-h) | 1.3M ep, 130K |
| 바퀴 이동 양팔 | 과제 복잡도 (설거지) | spine 고정/슬라이딩/굽힘, 몸통 45° pitch 유무 | spine·팔 길이, 어깨 오프셋 | Mink DiffIK | 50K ep, 5K |

- 목표 궤적: iPhUMI 로 76 개 (56 train / 20 test, 20 기술), 설거지는 UMI 양팔 데이터 (26 test).
- 품질: 2 초 미만 episode 필터, ViperX/ALOHA 는 작은 CMA-ES (pop 5, 3 세대) 로 **도달 가능한 형상 쪽으로 샘플 편향** + 모든 중간 후보를 데이터로 사용.
- PDP 식 노이즈 주입: 실행은 노이즈 섞인 명령, 라벨은 깨끗한 명령.

### 1.6 결과 수치 (확인할 기준값)

**Tracking Only, 궤적 평균** (논문 Table 2~5)

| 설정 | 방법 | Pos Err (cm) | 생존률 | 최적화 시간 |
|---|---|---|---|---|
| ViperX 단일 궤적 | Random | 19.4 | 72.2% | — |
| | CMA-ES | 5.0 | 98.3% | 47.5 s |
| | Zeroth | **4.1** | 96.1% | **0.5 s** |
| | DSG | **4.1** | **99.4%** | 2.8 s |
| ViperX 다중 궤적 | CMA-ES | 7.1 | 86.7% | 663.8 s |
| | DSG | **2.4** | 98.9% | 43.5 s |
| Quadruped 단일 | CMA-ES | **1.9** | 100% | 265.5 s |
| | Zeroth | 2.6 | 99.4% | 0.8 s |
| Bimanual 다중 (26 궤적) | CMA-ES | 1.7 | 100% | 11,505 s |
| | Zeroth | 1.8 | 99.6% | **20.7 s** |
| | DSG | 1.7 | 99.1% | 30.8 s |

- **Quadruped 는 CMA-ES 가 더 정확**하다. 학습 동역학이 낙상·접촉 불연속을 잘 못 맞춰서 예측-실제 보상 상관이 낮다 (Pearson r: ViperX 0.585, quadruped 0.535, bimanual 0.591).
- 교차 형상 제어 (Table 1, bimanual): Small 11.6M 6.6 cm / 98.9%, Large 63.6M 5.9 cm / 99.5%, Mink 오라클 4.8 cm / 100%. Quadruped 생성 로봇 제어는 RL 전문가와 r = 0.53.
- **실물 ALOHA 천 털기:** 원래 설계 대비 최대 관절속도 2.57 → 1.82 rad/s (-30%), 추적 오차 13.0 → 3.5 cm (-73%). 링크를 늘리고 팔을 **거꾸로 매달아** 아래에서 휘두르는 설계가 나왔다.
- 한계: primitive geometry 만, 테스트 시간 계산을 늘려도 ~1 분 이후 정체, RL 데이터 비용 큼.

---

## 2. 코드 지도 — 논문 ↔ 파일

| 논문 | 파일 |
|---|---|
| RoboToken 정의 (Table 19) | `t2/robotok/token.py` |
| tokenize / detokenize, 관성 분할, transform 정규화 | `t2/robotok/tokenizer.py` |
| 토큰 직렬화 | `t2/robotok/io.py`, zarr 스키마 `t2/io/schema.py` |
| 절차적 로봇 생성 문법 (§9.3.1) | `t2/robogen/components.py` + `assets/mjcf/*` 의 `<custom>` 블록 |
| design space 정의 | `t2/robogen/viperx.py`, `wheeled_bimanual.py`, `umi_on_legs_plus_plus.py` |
| 추적 환경 (MuJoCo) | `t2/env/track_env.py`, Mink 오라클 `t2/env/*mink_runner.py` |
| RL 전문가 (MJX + Brax PPO) | `t2/mjx_env/`, `scripts/train_rl_procedural.py` |
| DiT 백본 | `t2/model/core.py` |
| 토큰 타입별 adapter, 마스킹, 확산 task | `t2/model/modality.py`, `t2/model/task/task.py` |
| 모델 조립 / 체크포인트 로드 | `t2/model/t2.py` |
| 보상 함수, Zeroth-Order, DSG | `t2/eval/diffusion_guidance.py` |
| co-design 평가 루프 | `t2/eval/hardware_optimization.py`, `scripts/evaluate_hardware_opt*.py` |
| 교차 형상 제어 평가 | `t2/eval/ctrl.py`, `scripts/evaluate_ctrl.py` |
| 학습 | `scripts/train.py` + `config/train.yaml` + `config/addon_*.yaml` |
| CMA-ES 기준선 | `scripts/optimize_cmaes_*.py` |
| 설정 (Hydra) | `config/` — 명령줄에서 `key=value` 로 덮어씀 |

권장 코드 읽기 순서: `scripts/generate_robotoken_dataset.py` (50 줄) → `t2/robotok/token.py` → `t2/model/task/task.py` 의 forward →
`t2/eval/diffusion_guidance.py` 의 `HardwareOptimizer` / `ObjectiveGuidedDiffusion`.

---

## 3. 이 서버에서 달라지는 점 (먼저 읽을 것)

서버 사양 (2026-09-13 확인): **Tesla V100 32 GB** (GPU 0) + Quadro P620 2 GB, NVIDIA driver **575.64** (CUDA 12.9 까지),
CPU 96 코어, RAM 251 GB, `/mnt/gstore` 여유 2.9 TB, `uv` 미설치.

repo 는 **CUDA 13** 기준이다 (`pyproject.toml`: `jax[cuda13]`, PyTorch `cu130` 인덱스, `uv.lock`: torch 2.13.0, jax 0.10.2).
그대로 설치하면 이 서버에서는 GPU 를 못 쓴다.

| 문제 | 이유 | 대응 |
|---|---|---|
| CUDA 13 휠 | CUDA 13 은 driver ≥ 580 필요 **그리고 Volta(V100, sm 7.0) 지원을 제거**했다 | CUDA 12 계열 휠 사용 |
| repo 문서 경고 | "driver 가 CUDA 13 에 못 미치면 CUDA 휠이 **조용히 CPU 로 fallback**" | 설치 후 반드시 GPU 인식 확인 (0 단계) |
| PyTorch | cu126 인덱스에 `torch 2.14.0+cu126` 있음 (2.13 정식판은 cu126 에 없음). cu126 은 PyTorch 2.15 부터 중단 예정 | torch 는 cu126 인덱스로 |
| JAX | `jax-cuda12-plugin` 0.10.2 / 0.11.1 존재 | `jax[cuda12]` |
| flash attention | 코드가 `sdpa_kernel(SDPBackend.FLASH_ATTENTION)` 을 강제. PyTorch flash 커널은 sm 8.0 이상 | 추론 명령에 `use_flash_attn=false` |
| bf16 | V100 은 bfloat16 네이티브 미지원 | 추론 명령에 `use_mixed_precision=false` |
| `torch.compile` | `inference.yaml` 기본 `use_torch_compile: true`. 구형 GPU 에서 컴파일 오류가 날 수 있다 | 오류 시 `use_torch_compile=false` |
| 학습 규칙 | RL 폴더 공통 규칙: 학습은 tmux + 텔레그램 알림 | `bash ../scripts/tmux_run.sh <세션> "<명령>"` |

`use_flash_attn` / `use_mixed_precision` / `use_torch_compile` 은 `config/inference.yaml` 최상위 키라 모든 평가 스크립트에서
명령줄로 덮어쓸 수 있다 (`t2/model/t2.py` 가 체크포인트 설정 위에 이 값을 덮어씀). 이하 명령에서는 이 셋을 `$V100` 으로 줄여 쓴다.

```sh
export V100="use_flash_attn=false use_mixed_precision=false"
```

수학적으로 flash 와 일반 attention 은 같은 값을 내므로 결과는 같고 속도만 느려진다. fp32 는 bf16 보다 메모리를 2 배 쓰지만 공개 모델(11.6M~)은 32 GB 에 충분하다.

---

## 4. 단계별 실습

각 단계 형식: **목표 → 명령 → 나오는 것 → 확인할 것 → 배우는 것**. 모든 명령은 repo 루트에서 실행한다.

### 0 단계 — 환경 구성 (약 30 분)

**목표:** Python 3.12 + JAX/PyTorch/MuJoCo 가 V100 을 인식하는 가상환경.

```sh
cd /mnt/gstore/home/xiangyue/RL/transformer-transformer
curl -LsSf https://astral.sh/uv/install.sh | sh     # uv 설치 (~/.local/bin)
```

`pyproject.toml` 을 CUDA 12 로 바꾼다 (원본은 git 으로 되돌릴 수 있다: `git diff pyproject.toml`).

1. `"jax[cuda13]>=0.8.0; sys_platform == 'linux'"` → `"jax[cuda12]>=0.8.0; sys_platform == 'linux'"`
2. `[[tool.uv.index]]` 의 `name = "pytorch-cu130"`, `url = ".../whl/cu130"` → `pytorch-cu126`, `.../whl/cu126`
3. `[tool.uv.sources]` 세 줄의 `index = "pytorch-cu130"` → `"pytorch-cu126"`

```sh
uv lock                 # 잠금 파일을 CUDA 12 기준으로 다시 풀기 (torch 2.14 로 올라갈 수 있음)
uv sync --extra dev
source .venv/bin/activate
export WANDB_MODE=offline   # W&B 계정 없이 로컬에만 기록
```

**확인할 것:** 아래 세 줄이 모두 GPU 를 가리켜야 한다. `cpu` 만 나오면 다음 단계로 가지 말 것.

```sh
python -c "import torch; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0), torch.cuda.get_arch_list())"
python -c "import jax; print(jax.__version__, jax.devices())"
python -c "import mujoco; print(mujoco.__version__)"
```

- torch: `True`, `Tesla V100-PCIE-32GB`, arch 목록에 `sm_70`
- jax: `[CudaDevice(id=0), ...]`
- `uv lock` 이 버전 충돌로 실패하면 충돌 메시지의 패키지만 버전을 풀어 준다. 이 단계는 이 서버에서 아직 검증 전이다.

**배우는 것:** GPU 세대 ↔ CUDA 메이저 버전 ↔ driver ↔ 휠 인덱스의 호환 관계. JAX(MJX, RL 전문가)와 PyTorch(DiT)가 한 환경에 공존하는 구조.

### 1 단계 — RoboToken 을 손으로 만져 보기 (GPU 불필요, 약 10 분)

**목표:** "로봇 = 토큰 시퀀스" 가 실제로 손실 없이 왕복되는지 확인하고 토큰 파일을 열어 본다. 논문 §2.1.

```sh
pytest tests/robotok                     # 수백 개 무작위 로봇 + Menagerie 모델 왕복
```

**나오는 것:** `811 passed`. 각 로봇을 `preprocess → tokenize → detokenize` 한 뒤 질량행렬·관성·시뮬레이션 상태가 원본과 같은지 검사한다.

다음은 데이터 다운로드(2 단계)의 `data.zip` 이 필요하다. 2 단계를 먼저 해도 된다.

```sh
python scripts/generate_robotoken_dataset.py --pickle_path data/july25th2025-huy-20skills-train.pkl
python scripts/generate_mj_menagerie_dataset.py --pickle_path data/july25th2025-huy-20skills-train.pkl
```

**나오는 것:** `wheeled_bimanual.zarr` (무작위 이동 양팔 로봇 1 대의 토큰 + reset 1 회), `mj_menagerie.zarr` (Menagerie 11 대 + UMI on Legs).

**열어 볼 것:**

```python
import zarr
r = zarr.open("mj_menagerie.zarr", mode="r")
print(r.tree())
for g in ["link", "dyna_joint", "fixed_joint", "actuator"]:
    a = r[f"hardware/{g}"]
    print(g, a.shape, a.attrs.asdict())          # 속성 이름 → 폭 (알파벳 순으로 이어 붙음)
print(r["hardware_meta/link/ends"][:])            # 로봇별 link 토큰 누적 끝 인덱스
```

**확인할 것:** 로봇마다 `ends` 차이 = 토큰 수. Menagerie 로봇이 28~101 토큰인지, `attrs` 의 폭 합이 Table 19 와 맞는지.

**배우는 것:** Table 19 의 속성이 벡터에 어떻게 들어가는지 (binary / log / signed log 인코딩), zarr 3 규칙
(마지막 축 = 알파벳 순 속성 slab, `ends` 는 누적, root attrs = 생성 config 전체).

### 2 단계 — 공개 자산 다운로드 (약 7.5 GB)

```sh
sudo apt install libarchive-tools     # bsdtar 없을 때만
wget https://real.stanford.edu/transformer-transformer/SHA256SUMS
wget -qO- https://real.stanford.edu/transformer-transformer/checkpoints.zip | bsdtar -xvf- -C ./   # 7.26 GB
wget -qO- https://real.stanford.edu/transformer-transformer/data.zip        | bsdtar -xvf- -C ./   # 223 MB
```

지금은 받지 않는 것: `rl_policies.zip` (10.0 GB, quadruped RL 경로에만 필요), HF 학습 데이터셋 (37~646 GB).

**나오는 것:**

| 체크포인트 | design space | task | 크기 | 쓰는 단계 |
|---|---|---|---|---|
| `wheeled_bimanual/z4454nxj/045.pt` | 이동 양팔 | ctrl + hardware_gen | small 11.6M | 3 |
| `wheeled_bimanual/mgoc83ra/035.pt` | 이동 양팔 | hardware_gen | small | 4, 5 |
| `viperx/hw3n9bux/032.pt` | ViperX | hardware_gen | small | 6, 8 |
| `umi_on_legs_plus_plus/xrh4wk4l/040.pt` | quadruped | hardware_gen | small | (RL 자산 필요) |

`data/`: `july25th2025-huy-20skills-{train,test}.pkl` (단일 팔 궤적), `bimanual_dish_washing_test.pkl` (26 개 설거지 궤적).
궤적 pickle 형식: `list[np.ndarray]`, 각 `(T, n_arms, 4, 4)` 50 Hz SE(3).

체크포인트 폴더의 `.pt` 와 `cfg.pkl` 은 같은 폴더에 둔다 (평가 스크립트가 옆의 `cfg.pkl` 을 읽는다).

### 3 단계 — 하나의 모델로 처음 보는 로봇 제어 (§2.2 b, Table 1)

**목표:** 형상 토큰을 조건으로 받는 단일 제어기가 **학습에 없던** 로봇 5 대를 제어하는지 본다.

먼저 3 episode 만:

```sh
python scripts/evaluate_ctrl.py evals/ctrl@eval_fn=wheeled_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/z4454nxj/045.pt $V100 \
  eval_fn.num_processes=16 \
  'eval_fn.hardware_traj_pairs_generator.list1=[10000000]' \
  'eval_fn.hardware_traj_pairs_generator.list2=[0,1,2]'
```

되면 전체 (5 형상 × 26 궤적 = 130 episode):

```sh
python scripts/evaluate_ctrl.py evals/ctrl@eval_fn=wheeled_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/z4454nxj/045.pt $V100 eval_fn.num_processes=50
```

Ray 가 CPU 시뮬레이션 워커를 띄우고 GPU 에 정책 서버를 올려 관측을 배치로 처리한다.

**나오는 것:** 끝에 요약 지표, `wandb/latest-run/files/ctrl_eval_summary.zarr` (step 별 롤아웃).
다시 보기: `python scripts/summarize_rollout.py wandb/latest-run/files/ctrl_eval_summary.zarr`

**확인할 것:**

| 지표 | 뜻 | 기준 |
|---|---|---|
| `metric/pos_err/mean` | 위치 추적 오차 (m), **두 팔 합** | 논문 Table 1 은 팔당 평균 → **2 로 나눠** 비교. Small 6.6 cm |
| `metric/orn_err/mean` | 자세 오차 (rad) | Small 10.7° |
| `done/bad_termination/any` | 오차 0.5 m 초과 종료 비율 | 생존률 = 100 − 이 값, Small 98.9% |

**배우는 것:** 형상 조건부 behavior cloning (Mink 오라클 증류), 이질적인 관절 수를 토큰 수로 처리하는 방법,
"설계 검증에 학습 제어기를 써도 되는가" 라는 논문의 질문.

### 4 단계 — 동작을 주면 로봇이 나온다 (Zeroth-Order, §2.2 a)

**목표:** 설거지 궤적마다 로봇을 생성하고, 모델이 스스로 매긴 점수와 시뮬레이션 실제 점수를 비교한다.

빠른 확인 (궤적 2 개, seed 1):

```sh
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt $V100 \
  num_seeds_per_datapoint=1 'eval_fn.traj_indices=[0,1]'
```

**내부 동작:** 궤적 × seed 마다 후보 64 대를 한 배치로 100 step DDIM 확산 → 각 후보가 함께 생성한 상태·행동으로
보상(`tracking_only`)을 계산 → 최고 1 대 선택 → RoboToken 을 MJCF 로 복원 → Mink DiffIK 로 실제 궤적 추적.
로그: `value: ... for traj N, took ...s`.

**나오는 것 (W&B run 폴더):**
- `hardware_cache/<sha256>.pt` — 선택된 설계 토큰, `predicted_value`, `optimize_time` (같은 설정 재실행 시 캐시 사용)
- `hardware_opt_summary.zarr` — 생성 로봇들의 롤아웃

**확인할 것:**

| 지표 | 뜻 | 주의 |
|---|---|---|
| `validity` | 생성 토큰이 시뮬레이션 가능한 로봇으로 복원된 비율 | 1.0 에 가까워야 함 |
| `hardware_meta/predicted_value` | 모델이 예측한 자기 설계의 보상 | 8 step 합 |
| `hardware_meta/actual_value` | 시뮬레이션으로 다시 잰 보상 | episode 전체 (수백 step) 합 → **step 수로 나눠서** 비교 |
| `metric/pos_err/mean` | 생성 로봇의 추적 오차 | 두 팔 합 |
| `optimize_time` | 설계 1 개에 걸린 시간 | 논문은 RTX 4090 기준. V100 + fp32 는 더 느리다 |

논문의 bimanual 표(Table 5)와 수치를 맞추려면 **설계 1 개로 26 궤적 전부**를 푸는 설정을 쓴다:

```sh
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual_multitraj \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt $V100 \
  eval_fn.runner.infer_controller_weights=false
```

기준: Zeroth 1.8 cm (팔당, 코드 로그는 합이므로 약 3.6), 생존 99.6%. `infer_controller_weights=false` 가 논문 설정이다 (기본값 `true` 는 추적이 더 좋게 나온다).

**배우는 것:** 생성기 = 비평가, 테스트 시간 계산(후보 수) 스케일링, 확산 composition 으로 다중 궤적 최적화,
그리고 **예측 보상의 신뢰도가 이 방법의 상한**이라는 점 (§3 Fig. 7).

### 5 단계 — 학습에 없던 보상으로 설계 바꾸기 (zero-shot, §2.3, Fig. 6)

**목표:** 같은 모델에 보상만 바꿔 넣으면 다른 로봇이 나오는지, DSG 가 무엇을 바꾸는지 본다.

**5-a. 보상 교체 (Zeroth-Order 유지)**

```sh
for R in tracking_only tracking_velocity tracking_size_bimanual; do
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt $V100 \
  reward_fns@eval_fn.hardware_optimizer_fn.reward_fns=$R \
  num_seeds_per_datapoint=3 'eval_fn.traj_indices=[0,1,2,3]' \
  "tags=[bimanual,$R,unguided]"
done
```

보상 정의는 `config/reward_fns/` 에 있다. `tracking_velocity` 는 관절속도 페널티, `tracking_size_bimanual` 은 기하 길이 합이 5.0 m 를 넘으면 페널티.

**확인할 것:** 보상별로 생성 로봇의 크기(Table 16 기준 약 4.7 m), 관절속도(Table 12), 추적 오차가 어떻게 달라지는지.
6 단계 시각화나 `scripts/visualize_dataset.py dataset.path=<hardware_opt_summary.zarr>` 로 로봇 모양을 직접 비교한다 (GUI 필요).

**5-b. Dynamics Self-Guidance 켜기**

```sh
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt $V100 \
  hardware_optimizer@eval_fn.hardware_optimizer_fn=guided_diffusion \
  eval_fn.hardware_optimizer_fn.guidance.scale=0.2 eta=1.0 \
  eval_fn.hardware_optimizer_fn.num_seeds=4 \
  num_seeds_per_datapoint=3 'eval_fn.traj_indices=[0,1,2,3]' \
  'tags=[bimanual,tracking_only,guided]'
```

같은 명령에서 `hardware_optimizer@...=guided_diffusion`, `guidance.scale`, `eta` 를 빼면 Zeroth-Order 이다.
`num_seeds` 를 1, 4, 16, 64 로 바꿔 두 방법을 비교한다.

**확인할 것:** 후보 수가 적을 때 DSG 가 더 나은지 (논문: 샘플 1 개일 때 242 → 341), 후보가 많아지면 차이가 사라지는지, DSG 시간이 몇 배인지.
`guidance.scale` 을 너무 작게 하면 DSG 가 Zeroth-Order 와 똑같아지고, 너무 크게 하면 `validity` 가 떨어진다.

**배우는 것:** classifier guidance 를 로봇 설계에 적용하는 방법, 보상이 형상 토큰에 직접 걸릴 때 gradient 크기 문제 (계수 0.005),
확률적 DDIM(`eta`) 이 탐색에 주는 효과, "guidance 와 search 는 대체재" 라는 결론.

### 6 단계 — 노이즈에서 로봇이 생기는 과정 보기

```sh
python scripts/visualize_robotoken_diffusion.py \
  ckpt_path=checkpoints/viperx/hw3n9bux/032.pt \
  pickle_path=data/july25th2025-huy-20skills-test.pkl \
  num_inference_steps=100 traj_idx=0 output_cache_dir=diffusion_vis/ $V100
```

`num_inference_steps` 는 반드시 명시한다 (기본 `null`). 헤드리스 서버라 MuJoCo 렌더링에 `MUJOCO_GL=egl` 이 필요할 수 있다.

**나오는 것:** `diffusion_vis/hardware_diffusion_a{모터수}_d{관절수}_f{고정관절수}_s00.mp4` (denoising 진행 영상),
`..._states.pkl` (Blender 용 프레임), `<hash>.pt` (캐시). `traj_idx` 를 0, 1, 2 로 바꾸면 동작마다 다른 팔이 나온다.

**배우는 것:** 확산 초반에는 이산 구조(관절 수·장착 방향)가 흔들리다 후반에 굳는 과정, "생성 설계는 학습 분포 안에서만 보간된다" (§3 manifold adherence).

### 7 단계 (선택) — 작은 데이터를 만들고 직접 학습하기

**목표:** 데이터 생성 → 학습 → 평가 파이프라인 전체를 작은 규모로 한 바퀴 돈다. 논문 수준 성능은 기대하지 않는다.

**7-a. ViperX Mink 데이터 생성 (CPU 만 사용)**

```sh
python scripts/inference.py --config-name=datagen runner=mink 'env@runner.env=variable_viperx' \
  runner.env.pickle_path=data/july25th2025-huy-20skills-train.pkl \
  data_path=data/viperx_mink_small.zarr num_hardware=200 num_episodes_per_hardware=10 num_processes=48
```

형상 200 × 10 episode = 2,000 episode (논문 ViperX 는 3.8M). 로그의 `metric/pos_err/q50`, `done/bad_termination/any` 로
**무작위 ViperX 는 대부분 궤적에 닿지 못한다**는 것(논문 19.4 cm)을 직접 확인한다. 이것이 CMA-ES 편향 샘플링을 쓰는 이유다.
데이터를 눈으로 보려면 `python scripts/visualize_dataset.py dataset.path=data/viperx_mink_small.zarr` (GUI 필요).

**7-b. hardware_gen 학습**

`config/train.yaml` 의 `defaults` 에서 `addon_hardware_bimanual_diffusion` 을 `addon_hardware_viperx_diffusion` 으로 바꾼 뒤,
합성된 설정을 먼저 출력해서 V100 에서 꺼야 할 키 경로를 확인한다.

```sh
python scripts/train.py hardware_dataset_path=data/viperx_mink_small.zarr --cfg job | grep -n -i 'flash\|mixed_precision\|batch_size\|num_epochs'
```

`hardware_gen` task 는 flash attention 을 켜고(`config/model/tasks/hardware_gen.yaml`) bf16 을 요구한다. V100 에서는 둘 다 꺼야 하므로
출력에서 찾은 키로 `...use_flash_attn=false accelerator.mixed_precision=no` 를 붙인다. 짧게 돌리려면
`train.batch_size=128 train.num_epochs=5 train.num_batches_per_epoch=256`.

RL 폴더 규칙에 따라 학습은 tmux + 텔레그램 알림으로 띄운다:

```sh
bash ../scripts/tmux_run.sh t2_viperx_small \
  "cd transformer-transformer && source .venv/bin/activate && WANDB_MODE=offline python scripts/train.py hardware_dataset_path=data/viperx_mink_small.zarr <위에서 정한 override>"
```

**나오는 것:** `wandb/run-*/files/{cfg.pkl, 000.pt, ..., final.pt}`, 첫 실행 시 `data/viperx_mink_small.zarr.norm.pt` (정규화 통계).
로그의 `number of parameters` (small ≈ 11.6M), 토큰 타입별 loss (`hardware_gen/link`, `hardware_gen/actuator`, ...).

**확인할 것:** 어떤 토큰 타입 loss 가 늦게 떨어지는지. 학습한 체크포인트로 6 단계를 돌려 로봇이 "로봇 모양"으로 나오는지 (데이터가 작아서 `validity` 가 낮을 수 있다).

**배우는 것:** addon 의 `seq_len` 이 design space 를 정의하는 방식, 질량·관성·게인을 log 공간으로 두는 이유,
EMA 가중치 사용, 데이터 규모가 생성 품질에 미치는 영향.

논문 hyperparameter 표(Table 20: LR 5e-3, batch 64, 50 epoch)와 repo 기본값(`train.yaml`: LR 1e-4, batch 1024, 20 epoch × 1024 batch)이 다르다.
공개 체크포인트는 repo 설정으로 학습됐다고 문서가 명시하므로 repo 값을 따른다.

### 8 단계 (선택) — CMA-ES 기준선과 나란히 놓기

```sh
python scripts/optimize_cmaes_viperx.py 'tags=[viperx,tracking_only,cmaes]' reward_fns=tracking_only \
  max_fun=15 num_hardware_seeds_per_traj=1 'traj_indices=[0,1]' \
  pickle_path=data/july25th2025-huy-20skills-test.pkl

python scripts/evaluate_hardware_opt.py 'tags=[viperx,tracking_only,unguided]' \
  reward_fns@eval_fn.hardware_optimizer_fn.reward_fns=tracking_only \
  eval_fn.hardware_optimizer_fn.num_seeds=16 num_seeds_per_datapoint=1 'eval_fn.traj_indices=[0,1]' \
  ckpt_path=checkpoints/viperx/hw3n9bux/032.pt pickle_path=data/july25th2025-huy-20skills-test.pkl $V100
```

**확인할 것:** 같은 궤적에서 CMA-ES (실제 시뮬레이션 롤아웃으로 평가) 와 모델 (예측 동역학으로 평가) 의 추적 오차·시간.
여러 예산으로 돌린 뒤 `python scripts/plot_hardware_opt.py paths.results_root=./wandb` 로 Fig. 5 형태의 그림을 만든다
(태그·예산을 명령줄 인자로 넘겨야 그림에 분류된다, `docs/visualization.md` 참고).

**배우는 것:** 논문의 "수 초 vs 수 시간" 비교가 무엇을 재는지 — CMA-ES 는 후보마다 순차 롤아웃, 모델은 배치 축 + 시간 축(8 step) 병렬.

### 하지 않는 것 (비용)

| 작업 | 비용 | 대안 |
|---|---|---|
| Quadruped RL 전문가 학습 | 16 A100-h × 128 개 | `rl_policies.zip` (10 GB) |
| 논문 학습 데이터 재생성 | ViperX 3.8M episode | HF 데이터셋 다운로드 |
| 논문 규모 재학습 | ViperX 데이터 646 GB | bimanual 46 GB 세트로 축소 재현 |
| 실물 ALOHA 제작 | 하드웨어 | `rollout/ctrl` 의 `target_qpos` 재생 코드 참고 (`docs/data_generation.md`) |

---

## 5. 우리 quad co-design 과 비교 (2026-09-13 오후 갱신)

`quad/` 에서 하고 있는 co-design (허벅지·종아리·풀리비 + PPO) 과 구조를 나란히 놓으면:

| 항목 | quad (현재) | Transformer Transformer |
|---|---|---|
| 설계 변수 | 연속 2 + 이산 1 (풀리비) | 이산 topology + 연속 수십 개, 완전한 MJCF |
| 설계 탐색 | 하드웨어 정책 (Beta/categorical), score-function gradient. `thigh_term30` 100 outer 동안 분포가 거의 안 움직였다 (entropy `2.148 -> 2.137`, scale std `0.179` 유지) | 조건부 확산 생성 + 보상 순위 (후보 64) / gradient guidance |
| 설계 평가 | 매 outer 실제 Isaac Gym 롤아웃 (형상당 약 200 env × 500 step, DR on) | 모델이 생성한 8 step 동역학으로 예측 (실제 롤아웃은 검증용) |
| 제어기 | 형상 조건부 PPO 하나 (obs 에 형상 3 개) | 형상 조건부 BC 제어기 (전문가 증류) |
| 보상 변경 | 재학습 | 추론 시점 교체 |
| 목표 | 평지 최고 속도 (보행). 9-13 부터 다지형 학습 병행 | 말단장치 궤적 추적 (조작) |
| 지형 | 9-13 이전 run 은 사실상 평지 (±1 mm). `multi_terrain_20260913` 부터 rough 0~4 cm, slope 4.6~16도, 계단 2~6 cm curriculum | 해당 없음 (조작 과제) |
| 약점이 드러나는 곳 | 지형·접촉. 허벅지 접촉은 30 N 초과 시 종료로 처리 | 다리 로봇에서 예측 보상 상관 낮음 (r 0.535) — 같은 원인 |

현재 quad 상태 (2026-09-13):

- `thigh_term30_20260913` (평지, 허벅지 30 N 종료) 공유 정책 20-seed 비교 — 속도 Best `0.2518` / Nominal `0.2352` / Worst `-0.0360` m/s,
  완주 `0.9998` / `0.7953` / `0.0117`.
- 같은 정책을 학습에 없던 지형 5 종에서 녹화 (seed 0) — 대체로 Best 가 앞서지만 계단 내려가기 4 cm 에서는 Nominal 이 더 멀리 갔다.
- best 는 fitness 최대일 뿐, 분포가 수렴하지 않아 사실상 거의 무작위 후보 500 개 중 최고다.
- `multi_terrain_20260913` 학습 중. 진급을 평균속도 기준으로 바꾼 뒤 terrain level 이 0 에서 오르기 시작했다.

읽으면서 확인할 질문:

1. 우리 `evaluate_codesign_comparison.py` 의 "형상 obs 를 넣은 정책 하나" 는 이 논문 `ctrl` task 와 같은 아이디어다. 논문은 이 제어기를 검증기로 믿을 수 있는지를 상관계수로 따로 쟀다. 우리도 Best/Nominal/Worst 에서 공유 정책 성능과 형상별 전용 정책 성능의 상관을 재야 하는가. 평가 지형에 따라 순위가 바뀌는 것 (계단 내려가기) 도 같은 검증 문제다.
2. 논문 quadruped 는 이산 선택마다 RL 전문가를 따로 두고 연속 변수만 DR 로 덮었다. 우리 풀리비(이산)도 같은 방식이 필요한가.
3. 논문은 무작위 설계로 만든 데이터가 대부분 실패라 CMA-ES 로 샘플을 편향시켰다. 우리 Worst 형상 (`thigh_term30` outer 8, fitness `-59.3`, scale 1.36/1.28, 벨트비 1.2, 비교 완주 0.012) 이 학습 분포를 오염시키는가.
4. 확산 생성기를 쓰려면 형상별 롤아웃 데이터가 대량으로 필요하다. 우리 outer 100 × 5 형상 = 500 형상은 논문 bimanual(5K 형상)의 1/10 이다.
5. 우리 score-function gradient 는 100 outer 동안 분포를 거의 못 움직였다. 논문은 탐색을 후보 수 (search) 와 guidance 로 하고 "둘은 대체재" 라고 결론냈다. 우리도 outer 당 후보 5 개를 늘리는 search 쪽으로 보완해야 하는가.
6. 논문의 예측 동역학은 quadruped 의 낙상·접촉 불연속을 잘 못 맞췄다. 다지형 (계단·경사) 은 접촉 불연속이 더 많으므로, 생성 모델 방식으로 갈 경우 가장 약한 부분이 된다.

---

## 6. 용어

| 용어 | 뜻 |
|---|---|
| Embodiment | 로봇의 몸 (형상·관절·모터·물성) |
| RoboToken | 로봇 부품 하나 또는 한 시점 상태 하나를 나타내는 연속값 벡터 토큰 |
| DiT | Diffusion Transformer. 이미지 patch 대신 토큰 시퀀스를 denoise |
| DDIM | 결정적/확률적 확산 샘플러. `eta=0` 결정적, `eta=1` 확률적 |
| Zeroth-Order | gradient 없이 여러 개 뽑아서 가장 좋은 것 선택 |
| DSG | Dynamics Self-Guidance. 모델 자신이 예측한 동역학의 보상 gradient 로 확산 유도 |
| Diffusion composition | 여러 조건의 노이즈 예측을 평균해 모든 조건을 만족하는 샘플 생성 |
| Mink / DiffIK | MuJoCo 기반 미분 역기구학 제어기 (고정 베이스 로봇의 오라클) |
| UMI / iPhUMI | 로봇 없이 사람이 그리퍼를 들고 시연 궤적을 모으는 장치 |
| design space | `<custom>` 문법으로 정의한 이산 선택 + 연속 변수 공간 |
| addon | design space 와 task 를 고르는 Hydra config 조각 (`seq_len` 포함) |
| validity | 생성 토큰이 시뮬레이션 가능한 로봇으로 복원되는 비율 |

---

## 7. 진행 체크리스트와 기록 양식

- [ ] 0 GPU 인식: torch `sm_70` / jax `CudaDevice` 확인
- [ ] 1 `pytest tests/robotok` → `811 passed`, zarr 열어 토큰 수 확인
- [ ] 2 `checkpoints/`, `data/` 압축 해제, `.pt` 옆 `cfg.pkl` 확인
- [ ] 3 ctrl 3 episode → 전체 130 episode, `pos_err/2` 를 6.6 cm 와 비교
- [ ] 4 bimanual co-design 빠른 확인 → multitraj 논문 설정, `validity`, step 당 predicted vs actual
- [ ] 5 보상 3 종 비교, DSG vs Zeroth (`num_seeds` 1/4/16/64)
- [ ] 6 ViperX 확산 영상 3 개 (궤적 0/1/2)
- [ ] 7 (선택) 작은 데이터 + 짧은 학습
- [ ] 8 (선택) CMA-ES 비교

단계마다 아래를 남긴다.

```
## <단계> YYYY-MM-DD
- 명령:
- 소요 시간 / GPU 메모리:
- 핵심 수치: (논문 기준값과 나란히)
- 산출물 경로:
- 논문과 다른 점과 원인:
- 알게 된 것 (1~2 줄):
```

---

## 참고

- 논문 PDF: https://arxiv.org/abs/2607.25798 (본문 9 쪽 + 부록: 보상 정의 §8, RoboToken·모델·데이터 상세 §9, Table 19~20)
- 프로젝트 페이지 / 영상: https://transformer-transformer.github.io · https://youtu.be/TTyjvPVFbNw
- repo 문서: `docs/starter.md` (설치·평가), `docs/data_generation.md` (토큰화·오라클·CMA-ES·zarr 형식), `docs/training.md`,
  `docs/extending.md` (새 design space), `docs/visualization.md` (Blender, Fig. 5 재현과 guidance 설정표)
- 공개 자산: https://real.stanford.edu/transformer-transformer/ (checkpoints 7.26 GB, data 223 MB, rl_policies 10.0 GB)
- 학습 데이터셋: https://huggingface.co/datasets/hqhuy/transformer-transformer
- CUDA 13 의 Volta 지원 제거: [Tom's Hardware](https://www.tomshardware.com/pc-components/gpus/nvidia-to-drop-cuda-support-for-maxwell-pascal-and-volta-gpus-with-the-next-major-toolkit-release), [CUDA.jl #3079](https://github.com/JuliaGPU/CUDA.jl/issues/3079)
- JAX CUDA 12 휠: [JAX 설치 문서](https://docs.jax.dev/en/latest/installation.html), [CUDA 12 deprecation RFC](https://github.com/jax-ml/jax/discussions/31964)
- PyTorch cu126 과 Volta: [cu126 중단 공지](https://dev-discuss.pytorch.org/t/notice-cuda-12-6-wheels-will-no-longer-be-published-from-pytorch-2-15-drops-maxwell-pascal-volta/3432), [cu128 Volta 제거 RFC](https://github.com/pytorch/pytorch/issues/172351)
