# Transformer Transformer 따라하기 설명서

논문: *Transformer Transformer: A Unified Model for Motion-Conditioned Robot Co-design* ([arXiv 2607.25798](https://arxiv.org/abs/2607.25798))

목표 동작(손이 지나갈 궤적)을 주면, 그 동작에 맞는 로봇 전체(링크·관절·모터)를 만들어 주는 모델이다.

---

## 전체 흐름

```
토큰 양식 정하기 → design space 정하기 → 조립·토큰화 → 시범 데이터 만들기 → 모델 학습 → 사용
     (사람)              (사람)            (자동)          (자동, 전문가)       (자동)    조종 / 설계 / 보상 바꾸기
```

| 파트 | 한 줄 요약 | 결과물 | 이 설명서에서 |
|---|---|---|---|
| 1. 토큰 양식 | 로봇을 담을 숫자 표의 칸을 정한다 | `t2/robotok/token.py` | 확인 |
| 2. design space | 어떤 부품을 어떤 범위로 바꿔 볼지 정한다 | 부품 XML + 규칙 | 확인 |
| 3. 조립·토큰화 | 로봇을 조립해 숫자 표로 바꾼다 | zarr (몸 토큰) | 작게 해 봄 |
| 4. 시범 데이터 | 로봇마다 전문가가 궤적을 따라가게 해 녹화한다 | zarr (몸 + 움직임) | 선택 |
| 5. 모델 학습 | 녹화에 노이즈를 섞고 맞히기를 반복한다 | 체크포인트 `.pt` | 한 step 보기, 실제 학습은 선택 |
| 6. 사용 | 로봇 조종, 로봇 설계, 보상 바꾸기 | 평가 결과 | 공개 체크포인트로 실행 |

파트 4·5 는 비용이 매우 크다. 그래서 저자가 학습해 둔 체크포인트를 받아 파트 6 을 한다.

---

## TODO — `docs/starter.md`

### Setup

- [x] `uv` 설치
- [x] Python 3.12 `.venv` 설치·활성화
- [x] PyTorch·JAX의 RTX 4070 SUPER 인식 확인
- [x] RoboToken smoke test (`811 passed`)
- [x] W&B offline 모드 사용

### Checkpoints & Data

- [x] `data.zip` SHA256 검증·압축 해제 (`data/*.pkl`)
- [x] `checkpoints.zip` SHA256 검증·압축 해제 (15 checkpoints)

### Evaluation

- [x] Cross-embodiment Control — 전체 130 episode
- [x] Control 결과 요약 (`ctrl_eval_summary.zarr`)
- [x] Motion-to-robot Co-design — Zeroth-Order, 궤적 0, 후보 64대
- [x] DSG Co-design — 궤적 0, 후보 4대와 64대
- [x] Co-design 결과 요약 (`hardware_opt_summary.zarr`)

### Visualization

- [x] Blender 5.1.0·FFmpeg 7.0.2 준비
- [x] 이동 양팔 Blender 템플릿 확인
- [x] 일반 diffusion 생성 영상 확인
- [x] DSG 최종 rollout을 Blender용 pickle로 변환
- [ ] DSG 최종 로봇 1프레임 시험 렌더링
- [ ] DSG 최종 rollout 전체 영상 렌더링

---

## 파트 0. 준비

**0-1. 환경 설치 (처음 한 번)**

이 컴퓨터의 현재 기준은 다음과 같다.

| 항목 | 현재 환경 |
|---|---|
| OS | Ubuntu 22.04.5 LTS (x86_64) |
| CPU / RAM | 16 logical CPU / 30 GiB RAM |
| GPU | NVIDIA GeForce RTX 4070 SUPER |
| NVIDIA driver | 595.91.07 |
| 프로젝트 경로 | `/home/cgr-ugrad-2026/quad_ws/transformer-transformer` |
| Python / CUDA | `.python-version` 의 Python 3.12 / 이 워크스페이스의 CUDA 12 lock |

PyTorch 2.14.0+cu126과 JAX 0.10.2 모두 RTX 4070 SUPER를 정상 인식한다.

이 워크스페이스는 이미 `pyproject.toml`과 `uv.lock`이 CUDA 12로 바뀐 상태이다. 다시 `sed`나 `uv lock`을 할 필요가 없다.

```sh
cd /home/cgr-ugrad-2026/quad_ws/transformer-transformer
curl -LsSf https://astral.sh/uv/install.sh | sh
export PATH="$HOME/.local/bin:$PATH"
uv sync --extra dev
source .venv/bin/activate
python -c "import sys, torch; print(sys.version); print(torch.cuda.is_available(), torch.cuda.get_device_name() if torch.cuda.is_available() else 'CPU only')"
python -c "import jax; print(jax.devices())"
pytest tests/robotok
```

- 현재 전역 `python` 3.14는 쓰지 않고, `uv`가 `.python-version`을 읽어 `.venv`에 Python 3.12를 설치한다.
- GPU 확인은 `True`, `NVIDIA GeForce RTX 4070 SUPER`, JAX `CudaDevice` 순서로 나와야 한다.

**0-2. 새 터미널을 열 때마다**

```sh
cd /home/cgr-ugrad-2026/quad_ws/transformer-transformer
source .venv/bin/activate
export WANDB_MODE=offline
export CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=0
export MUJOCO_GL=egl
```

- 모든 명령은 repo 루트에서 실행한다.
- RTX 4070 SUPER에서는 기존 flash attention·bf16 기본값을 쓴다. 예전 V100용 끄기 옵션은 붙이지 않는다.
- 데이터 생성의 `num_processes`는 16 CPU를 다 채우지 않게 일단 12로 시작한다.
- `GLFWError`, `Gym has been unmaintained` 경고는 무시한다.

**0-3. 자산 다운로드**

체크포인트(7.3 GB)와 궤적 데이터를 받는다. 오래 걸리므로 tmux 안에서 하고, 끊겨도 이어받는 `wget -c`를 쓴다.

```sh
tmux new -s t2_download
cd /home/cgr-ugrad-2026/quad_ws/transformer-transformer
wget -c https://real.stanford.edu/transformer-transformer/data.zip
wget -c https://real.stanford.edu/transformer-transformer/checkpoints.zip
unzip -tq data.zip && unzip -tq checkpoints.zip
bsdtar -xf data.zip -C ./
bsdtar -xf checkpoints.zip -C ./
```

- tmux 에서 나오기: `Ctrl+b` 다음 `d`. 다시 들어가기: `tmux attach -t t2_download`
- `unzip -tq` 검사가 통과하기 전에는 압축을 풀지 않는다.
- `data.zip`은 사람 시범 궤적 `*.pkl`이지 논문의 수십 GB 학습 Zarr가 아니다. 학습 Zarr는 파트 4에서 따로 다룬다.
- 현재는 압축 해제와 SHA256 검증까지 끝났고, 원본 ZIP은 공간 확보를 위해 정리했다.

**현재 결과:** `checkpoints/` 7.3 GB (체크포인트 15개), `data/` 287 MB (pickle 9개)

---

## 파트 1. 토큰 양식 — 로봇을 숫자 표로 담는 규칙

```
로봇 MJCF → 부품마다 한 줄 → 표 4 장 (Link / DynamicJoint / FixedJoint / Actuator)
```

- 표의 종류와 칸은 사람이 `t2/robotok/token.py` 에 한 번 정한다. 모든 로봇이 같은 칸을 쓴다.
- 로봇마다 행 수만 다르고, 부품끼리는 이름 대신 행 번호로 가리킨다.
- `tokenize` (MJCF → 표) 와 `detokenize` (표 → MJCF) 는 서로 되돌릴 수 있다.

```sh
pytest tests/robotok                          # 표로 바꿨다가 되돌려도 같은 로봇인지 검사
python explore/step1_tokenize_one_robot.py    # UR5e 한 대가 표가 되는 과정 출력
python explore/step1_edit_token.py            # 표의 숫자를 바꾸면 로봇이 바뀌는지 사진으로 확인
```

**결과:** `explore/out/ur5e_tokens/*.csv`, `explore/out/step1_edit_token.png`

---

## 파트 2. design space — 어떤 로봇들을 만들어 볼지

```
부품 XML + 붙일 부품 후보(_mount) + 숫자 범위(<numeric>) → 조립기 → seed 마다 로봇 1 대
```

- 사람이 정한다. 논문의 design space 는 ViperX 팔, 4족 + 팔, 이동 양팔 3 개다.
- 바뀌는 것은 부품 조합, 링크 길이, 장착 위치·방향, 관절 축이다. 모터 세기는 고정이다.

```sh
python explore/step1_generated_robots.py      # 이동 양팔 design space 에서 seed 0, 1, 2 로 조립
```

**결과:** `explore/out/generated/seed_N.xml`, `seed_N_tokens/*.csv`, `robots.png`

---

## 파트 3. 조립·토큰화 — 로봇을 zarr 에 담기

```
seed → 로봇 조립 → 전처리 → MuJoCo 로 불러오기 → 표 채우기 → zarr 저장
```

- 여기서는 로봇 1 대(이동 양팔)와 11 대(Menagerie)로 형식만 확인한다. 1 step 만 움직이고 학습에는 쓰지 않는다.
- 같은 경로로 다시 실행하면 덮어쓰지 않고 뒤에 이어 쓴다.

먼저 `scripts/generate_mj_menagerie_dataset.py` 에서 Cassie 줄을 주석 처리한다.

```sh
python scripts/generate_robotoken_dataset.py --pickle_path data/bimanual_dish_washing_train.pkl
python scripts/generate_mj_menagerie_dataset.py --pickle_path data/july25th2025-huy-20skills-train.pkl
python explore/step1_inspect_zarr.py          # zarr 안 구조 보기
```

**결과:** `wheeled_bimanual.zarr`, `mj_menagerie.zarr`

---

## 파트 4. 시범 데이터 — 전문가가 따라가는 모습 녹화

```
로봇 N 대 × 사람 손 궤적 → 전문가(Mink)가 따라감 → 몸 + 목표 + 실제 움직임 + 명령 → zarr
```

- 사람 손 궤적은 `data/*.pkl` 이다 (50 Hz 위치·방향).
- zarr 하나에 여러 로봇의 몸(`hardware/`)과 여러 episode 의 움직임(`rollout/`)이 이어 붙어 있다.
- 논문 규모는 이동 양팔 로봇 5,000 대 × 10 episode (37~48 GB, HuggingFace) 이고 받지 않았다.
- 여기서는 선택이다. ViperX 로봇 200 대 × 10 episode 를 CPU 로 만들어 본다.

```sh
python scripts/inference.py --config-name=datagen runner=mink 'env@runner.env=variable_viperx' \
  runner.env.pickle_path=data/july25th2025-huy-20skills-train.pkl \
  data_path=data/viperx_mink_small.zarr num_hardware=200 num_episodes_per_hardware=10 num_processes=12
```

**결과:** `data/viperx_mink_small.zarr`

직접 생성하지 않고 논문의 이동 양팔 학습 데이터(46 GB)를 그대로 보려면:

```sh
wget -c -P training_data/ \
  https://huggingface.co/datasets/hqhuy/transformer-transformer/resolve/main/6c5628-bimanual-dishwashing-train-10-1000-perchoice.zarr.tar
tar -xf training_data/6c5628-bimanual-dishwashing-train-10-1000-perchoice.zarr.tar -C data/
```

- 현재 여유 공간(448 GB)으로 이 데이터는 가능하다.
- ViperX 전체 학습 데이터는 압축 해제 후 약 646 GB라서 이 디스크에는 넣지 않는다.

---

## 파트 5. 모델 학습 — 노이즈 맞히기 반복

```
샘플 뽑기 → 일부 토큰에 노이즈 섞기 → 모델이 노이즈 예측 → 틀린 만큼 가중치 수정 → 반복 (약 2 만 번) → 체크포인트
```

| task | 보여 주는 것 | 맞히는 것 | 배우는 능력 |
|---|---|---|---|
| `hardware_gen` | 목표 궤적 | 몸 + 움직임 + 명령 | 동작 → 로봇 설계 |
| `ctrl` | 몸 + 현재 상태 + 목표 | 명령 | 처음 보는 로봇 조종 |

- 샘플 하나 = 로봇 몸 + 그 로봇 episode 의 몇 시점. 토큰에 "몇 번 관절 / 모터 / 손, 몇 step" 번호표를 붙여 한 줄로 넣는다.
- 여기서는 학습 한 step 을 실제 데이터로 본다. 실제 학습은 선택이다.

학습 한 step 보기 (6-2 를 한 번 실행해 결과 zarr 가 있어야 한다):

```sh
python explore/step2_train_trace.py
```

선택 — 파트 4 데이터로 짧게 학습:

1. `config/train.yaml` 의 `defaults` 에서 `addon_hardware_bimanual_diffusion` 을 `addon_hardware_viperx_diffusion` 으로 바꾼다.
2. RTX 4070 SUPER의 기본 flash attention·bf16 설정을 그대로 쓰고 tmux에서 실행한다.

   ```sh
   tmux new -s t2_viperx_small
   cd /home/cgr-ugrad-2026/quad_ws/transformer-transformer
   source .venv/bin/activate
   WANDB_MODE=offline CUDA_VISIBLE_DEVICES=0 python scripts/train.py \
     hardware_dataset_path=data/viperx_mink_small.zarr \
     train.batch_size=128 train.num_epochs=5 train.num_batches_per_epoch=256
   ```

**결과:** `wandb/run-*/files/{cfg.pkl, 000.pt, ...}`

---

## 파트 6. 사용 — 공개 체크포인트로 결과 확인

이번 실습에서 사용한 두 task와 검증의 관계는 다음과 같다.

| 단계 | 고정 입력 | 모델이 만드는 것 | 최종 확인 |
|---|---|---|---|
| Cross-embodiment `ctrl` | 로봇 형상 + 현재 상태 + 목표 모션 | 다음 모터 action | 주어진 처음 보는 로봇이 목표를 추적하는지 |
| Co-design `hardware_gen` | 목표 모션 | 하드웨어 + 예상 상태 + 예상 action | 선택된 하드웨어를 MJCF로 만들고 Mink로 다시 제어 |

- Co-design은 형상만 따로 평가하지 않고, 그 형상에서 예상되는 action·동역학·보상을 함께 고려해 설계하므로 co-design이라고 부른다.
- 최종 MuJoCo 검증은 생성 중 예상한 action을 재생하지 않는다. 생성된 하드웨어에서 Mink가 목표 모션을 추적하도록 새 action을 계산한다.
- DSG는 새 모델을 학습하는 방법이 아니라, `hardware_gen` 추론 중 매 denoising step을 보상 gradient 방향으로 보정하는 방법이다.

### 6-1. 노이즈에서 로봇이 생기는 과정 (`hardware_gen`)

```
목표 궤적 → 노이즈로 채운 표 → 노이즈 걷어 내기 100 번 → 로봇 표 → MJCF → 영상
```

```sh
python scripts/visualize_robotoken_diffusion.py \
  ckpt_path=checkpoints/viperx/hw3n9bux/032.pt \
  pickle_path=data/july25th2025-huy-20skills-test.pkl \
  num_inference_steps=100 traj_idx=0 output_cache_dir=diffusion_vis/
```

- 렌더링 중에는 출력이 없다. `Saved video` 가 나올 때까지 기다린다.
- 창은 뜨지 않는다. VS Code 탐색기에서 mp4 를 우클릭 → Download 로 받아서 본다.
- 다시 생성하려면 `diffusion_vis/*.pt` 캐시를 지운다.

**결과:** `diffusion_vis/hardware_diffusion_*.mp4`
**확인:** `traj_idx` 를 1, 2 로 바꾸면 다른 팔이 나오는지

### 6-2. 처음 보는 로봇 조종 (`ctrl`)

```
학습에 없던 로봇 몸 + 현재 상태 + 목표 → 모터 명령 → 시뮬레이터 한 step → 반복
```

먼저 3 episode (로봇 1 대 × 궤적 3 개):

```sh
python scripts/evaluate_ctrl.py evals/ctrl@eval_fn=wheeled_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/z4454nxj/045.pt use_torch_compile=false \
  eval_fn.num_processes=3 \
  'eval_fn.hardware_traj_pairs_generator.list1=[10000000]' \
  'eval_fn.hardware_traj_pairs_generator.list2=[0,1,2]'
```

잘 되면 전체 130 episode (로봇 5 대 × 궤적 26 개, 오래 걸리므로 tmux 권장):

```sh
python scripts/evaluate_ctrl.py evals/ctrl@eval_fn=wheeled_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/z4454nxj/045.pt use_torch_compile=false eval_fn.num_processes=12
```

- `use_torch_compile=false` 는 꼭 붙인다 (Ray 가 compile 된 모델을 넘기지 못함).
- 이 서버의 `scripts/evaluate_ctrl.py` 는 `z4454nxj` 가 통과하도록 sampler 검사를 고친 상태다.

**결과:** `wandb/latest-run/files/ctrl_eval_summary.zarr`
다시 보기: `python scripts/summarize_rollout.py wandb/latest-run/files/ctrl_eval_summary.zarr`

**이번 실행:** `wandb/offline-run-20260915_144103-nt9dyf6s/files/ctrl_eval_summary.zarr`

| 지표 | 결과 |
|---|---:|
| 위치 오차 평균 (두 팔 합) | 0.096 m |
| 자세 오차 평균 | 0.300 rad |
| 보상 평균 | 0.493 |
| bad termination | 11.5% |
| timeout | 88.5% |
| unstable | 0.0% |

**확인:**

| 지표 | 뜻 |
|---|---|
| `metric/pos_err/mean` | 위치 오차 (m, 두 팔 합) |
| `metric/orn_err/mean` | 자세 오차 (rad) |
| `done/bad_termination/any` | 오차가 0.5 m 를 넘어 끊긴 비율 |

### 6-3. 동작 → 로봇 설계 (`hardware_gen` + 검증)

```
목표 궤적 → 후보 64 대 생성 (몸 + 움직임) → 예측 점수로 1 대 선택 → MJCF → Mink 로 실제 따라가 검증
```

이번에 실행한 빠른 확인 (궤적 0, 후보 64대):

```sh
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt \
  num_seeds_per_datapoint=1 'eval_fn.traj_indices=[0]'
```

잘 되면 논문 설정 (설계 1 개로 26 궤적):

```sh
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual_multitraj \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt \
  eval_fn.runner.infer_controller_weights=false
```

**결과:** W&B run 폴더의 `hardware_cache/*.pt`, `hardware_opt_summary.zarr`

**이번 Zeroth-Order 실행:** 궤적 0 × 평가 seed 1개, 내부 후보 64대

- 결과: `wandb/offline-run-20260915_153127-supu5y6s/files/hardware_opt_summary.zarr`
- 위치 오차 0.025 m, 자세 오차 0.091 rad, 보상 평균 0.909
- bad termination 0%, timeout 100%, unstable 0%
- 모델 예측 최적화 값 7.56, 후보 생성·선택 6.83초

**확인:**

| 지표 | 뜻 |
|---|---|
| `validity` | 생성한 표가 시뮬레이션 가능한 로봇이 된 비율 |
| `hardware_meta/predicted_value` | 모델이 예측한 점수 |
| `hardware_meta/actual_value` | 시뮬레이션으로 잰 실제 점수 |
| `metric/pos_err/mean` | 추적 오차 |

### 6-4. 보상 바꾸기, DSG

```
보상 바꾸기:  6-3 의 점수 계산만 교체 → 재학습 없이 다른 로봇
DSG:          노이즈를 걷어 낼 때마다 점수가 오르는 쪽으로 살짝 밀기
```

보상 바꾸기:

```sh
for R in tracking_only tracking_velocity tracking_size_bimanual; do
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt \
  reward_fns@eval_fn.hardware_optimizer_fn.reward_fns=$R \
  num_seeds_per_datapoint=3 'eval_fn.traj_indices=[0,1,2,3]' \
  "tags=[bimanual,$R,unguided]"
done
```

| 보상 | 내용 |
|---|---|
| `tracking_only` | 궤적 추적만 |
| `tracking_velocity` | + 관절 속도 페널티 |
| `tracking_size_bimanual` | + 로봇 크기 페널티 |

DSG:

```sh
python scripts/evaluate_hardware_opt.py --config-name=evaluate_hardware_opt_bimanual \
  ckpt_path=checkpoints/wheeled_bimanual/mgoc83ra/035.pt \
  hardware_optimizer@eval_fn.hardware_optimizer_fn=guided_diffusion \
  eval_fn.hardware_optimizer_fn.guidance.scale=0.2 eta=1.0 \
  eval_fn.hardware_optimizer_fn.num_seeds=64 \
  num_seeds_per_datapoint=1 'eval_fn.traj_indices=[0]' \
  'tags=[bimanual,tracking_only,guided]'
```

**확인:** 보상마다 로봇이 달라지는지. `num_seeds` 를 1, 4, 16, 64 로 바꿔 후보가 적을 때 DSG 가 더 나은지.

**이번 DSG 실행 결과 (궤적 0 × 평가 seed 1개):**

| 후보 수 | 결과 경로 | 위치 오차 | 자세 오차 | 보상 평균 | 예측 최적화 값 |
|---:|---|---:|---:|---:|---:|
| 4 | `wandb/offline-run-20260915_155952-q3c7endl/files/hardware_opt_summary.zarr` | 0.030 m | 0.110 rad | 0.870 | 7.06 |
| 64 | `wandb/offline-run-20260915_160613-4u17f8ac/files/hardware_opt_summary.zarr` | 0.030 m | 0.050 rad | 0.900 | 7.50 |

둘 다 bad termination 0%, timeout 100%, unstable 0%였다. 이 한 궤적만으로 DSG와 Zeroth-Order의 일반적인 우열을 결론 내리지는 않는다.

### 6-5. 평가 rollout 시각화

Blender와 FFmpeg는 `.venv/bin`에서 바로 실행되도록 준비했고, 공식 이동 양팔 템플릿도 확인했다.

```sh
blender --version | head -n 1   # Blender 5.1.0
ffmpeg -version | head -n 1     # FFmpeg 7.0.2
```

DSG 후보 64대 실행의 최종 rollout을 Blender 입력 pickle로 변환했다.

```sh
mkdir -p comparison/dsg_pickle
python scripts/visualize_dataset.py \
  dataset.path=wandb/offline-run-20260915_160613-4u17f8ac/files/hardware_opt_summary.zarr \
  'dataset.rollout_groups=[ctrl,track_link_obs,dyna_joint_obs,actuator_obs,target_pose]' \
  dataset.use_cached_indices=false \
  pickle_output_root=comparison/dsg_pickle \
  visualization.disable_gui=true visualization.dt=0.0 gravcomp=true
```

**현재 결과:** `comparison/dsg_pickle/ep00_hw941733317.pkl`

먼저 1프레임만 렌더링해 형상과 카메라를 확인한다.

```sh
python scripts/render_pickle.py \
  --pickle comparison/dsg_pickle/ep00_hw941733317.pkl \
  --output comparison/dsg_preview \
  --template blender_templates/vis_template_wheeled_bimanual.blend \
  --resolution 640x480 --samples 1 --start-frame 1 --end-frame 1
```

> `visualize_dataset.py`의 target time ID 보정은 데이터마다 다르다. `hardware_opt_summary.zarr`에는 `time_idx`를 쓰고, 현재 control 결과에는 `time_idx + 4`를 쓴다.

`visualize_robotoken_diffusion.py`의 기존 영상은 일반 diffusion 과정이다. 현재 스크립트는 DSG의 보상 gradient와 중간 step을 저장하지 않으므로, 이미 끝난 DSG 실행에서 denoising 과정을 소급해 영상으로 만들 수는 없다. 지금 만드는 영상은 **DSG로 선택된 최종 로봇의 MuJoCo rollout**이다.

### 6-6. (선택) 기존 방식 CMA-ES 와 비교

```
CMA-ES: 후보마다 실제 시뮬레이션 → 점수 → 다음 후보 (반복)     vs     모델: 예측 점수로 바로 선택
```

```sh
python scripts/optimize_cmaes_viperx.py 'tags=[viperx,tracking_only,cmaes]' reward_fns=tracking_only \
  max_fun=15 num_hardware_seeds_per_traj=1 'traj_indices=[0,1]' \
  pickle_path=data/july25th2025-huy-20skills-test.pkl
```

```sh
python scripts/evaluate_hardware_opt.py 'tags=[viperx,tracking_only,unguided]' \
  reward_fns@eval_fn.hardware_optimizer_fn.reward_fns=tracking_only \
  eval_fn.hardware_optimizer_fn.num_seeds=16 num_seeds_per_datapoint=1 'eval_fn.traj_indices=[0,1]' \
  ckpt_path=checkpoints/viperx/hw3n9bux/032.pt pickle_path=data/july25th2025-huy-20skills-test.pkl
```

그래프: `python scripts/plot_hardware_opt.py paths.results_root=./wandb`

**확인:** 두 방법의 오차와 걸린 시간

---

## 문제가 생기면

| 증상 | 해결 |
|---|---|
| GPU 가 안 잡힘 (`False`, CPU 로 돌아감) | `nvidia-smi`와 `/dev/nvidia*`를 확인하고, 드라이버 변경 직후라면 재부팅 후 다시 확인 |
| flash attention / bfloat16 오류 | 임시로 `use_flash_attn=false use_mixed_precision=false`를 붙여 원인을 분리 |
| `torch.compile` 오류 | 명령 끝에 `use_torch_compile=false` 추가 |
| `Could not serialize the argument ... policy_server`, `cannot pickle 'DynamoTLS'` (6-2) | Ray 가 `torch.compile` 로 감싼 모델 함수를 넘기지 못함. 명령에 `use_torch_compile=false` 추가 |
| 렌더링 오류, mp4 가 안 생김 | `export MUJOCO_GL=egl` 했는지 확인 |
| `pose_traj has 1 end effectors, expected 2` | 양팔 로봇에는 `bimanual_*.pkl` 궤적을 쓴다 |
| `Dynamic foints can only be slide or hinge` | `generate_mj_menagerie_dataset.py` 에서 Cassie 줄 주석 처리 |
| 데이터 생성 실패 후 다시 돌려도 안 됨 | 빈 `*.zarr` 폴더를 지우고 다시 실행 |
| 스크립트를 못 찾음 | repo 루트에서 실행했는지 확인 |
| 6-2 `evaluate_ctrl.py` 에서 `line 43 ... AssertionError` | `z4454nxj` 는 `RandomTimestepSampler` 로 학습돼 스크립트의 sampler 검사에 걸린다. `scripts/evaluate_ctrl.py` 의 검사를 "`rollout_steps` 가 1 이면 하위 클래스도 허용" 으로 고친다 (이 서버는 수정 완료) |

---

## 용어

| 용어 | 뜻 |
|---|---|
| MJCF | MuJoCo 로봇 XML 파일 |
| RoboToken | 로봇 부품 1 개 또는 한 시점 상태를 나타내는 숫자 벡터 |
| zarr | 큰 배열을 압축 저장하는 폴더 형식 |
| 확산 모델 | 노이즈에서 조금씩 결과를 만들어 가는 생성 모델 |
| Zeroth-Order | 후보를 여러 개 생성해 가장 좋은 것을 고르는 방식 |
| DSG | 보상의 gradient 로 생성 과정을 유도하는 방식 |
| validity | 생성한 토큰이 시뮬레이션 가능한 로봇이 되는 비율 |
| Mink | MuJoCo 역기구학 제어기 (데이터 생성용 전문가) |


---

## 참고

- 프로젝트 페이지: https://transformer-transformer.github.io
- repo 문서: `docs/starter.md`, `docs/data_generation.md`, `docs/training.md`
- 공개 자산: https://real.stanford.edu/transformer-transformer/
